{
  "name": "dsh-plugins",
  "private": true,
  "description": "Plugins for DeepSeek Harness (dsh). One directory per plugin; what each one extends is declared by dsh.category — tools/ capabilities, runtime/ harness behaviour, ui/ web client.",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test 'packages/*/test/*.test.js' 'test/*.test.js'"
  }
}
