{
  "name": "dsh-plugins",
  "private": true,
  "description": "Plugins for DeepSeek Harness (dsh), grouped by extension shape: tools/ give the model capabilities, runtime/ changes harness behavior, ui/ extends the web client.",
  "license": "MIT",
  "workspaces": [
    "packages/*/*"
  ],
  "scripts": {
    "test": "node --test 'packages/*/*/test/*.test.js' 'test/*.test.js'"
  }
}
