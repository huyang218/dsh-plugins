# seal

[English](README.md) · 中文

给 PDF 盖章:**合同章**(在指定页面盖上印章)和**骑缝章**(一枚章按页数等分,盖在每页边缘,
抽页、换页、插页都会在拼起来时留下缺口)。

> [!WARNING]
> **盖上去的是一张图片,不是电子签名。** 它不绑定身份,也不能证明此后文件没被改动——拿到
> 文件的人可以把这枚章原样贴到别的文档上。《电子签名法》意义上的**可靠电子签名**还需要:
> CA 签发的证书把签署人绑定住,以及对文件字节做的密码学签名(PAdES)。
>
> 这个插件负责渲染"大家期望看到的那枚章";如果文件要能在争议中站得住,它还得被真正地
> **签**一次。插件会把这句话写进系统提示词,模型在调用前就知道,不会把盖章说成签署。

印章图片是**你自己的**(带透明背景的 PNG)。这两个工具不会为任何组织生成或绘制印章。

## 两个工具

### `seal_stamp` — 合同章

```
seal_stamp(pdf_path, pages="last", anchor="bottom-right", width_mm=40, rotation=-8)
```

- **页面**:`"3"`、`"1,4"`、`"2-5"`、`"all"`、`"first"`、`"last"`。同一页写两遍只盖一次
  ——印章是半透明的,盖两次看得出来。
- **位置**:命名锚点(`bottom-right`、`bottom-left`、`bottom-center`、`top-right`、
  `top-left`、`center`)配 `margin_mm`,或者直接给 `x_mm` / `y_mm`(从左下角量,和 PDF
  坐标系一致)。
- **尺寸**:只给直径,另一边按图片比例算——圆章不会被压扁成椭圆(压扁的章一眼就假)。
- **越界不自动纠正**:章跑到页面外面会**如实报告是哪几条边**,而不是悄悄挪回来。挪回来
  意味着章盖在了签署人没选的位置。
- **旋转**:绕锚点旋转,所以报告的坐标是旋转前的锚点,角度大时看一眼成品。

### `seal_straddle` — 骑缝章

```
seal_straddle(pdf_path, edge="right", width_mm=40, pages_per_seal=20)
```

一枚章按组内页数等分,每页在边缘留一条。**页数多要分组**:60 页盖一枚章,每页只剩不到
1 毫米的一线,既看不出也证明不了什么——线下做法同样是分批盖,`pages_per_seal` 就是那个批量。
组尾只剩一页时会并入上一组:一页的"骑缝"就是整枚章,什么都证明不了。

实现上,每页画的是**完整的章**,但用裁剪窗口只露出属于这一页的那一条。不这么做而是把章画到
页面外面靠边界"裁掉",内容其实仍然留在文件里,能被提取出来——那不叫裁掉。

## 安装

```sh
dsh plugin --profile web add dsh-plugin-seal
```

然后在插件设置里把 `sealPath` 指向你的印章 PNG。

## 配置

| 键 | 默认值 | 决定什么 |
| --- | --- | --- |
| `sealPath` | *(空)* | 默认印章图片;留空则每次调用都要显式传 `seal_path` |
| `widthMm` | `40` | 印章直径(国内公章常见 40mm) |
| `opacity` | `0.9` | 不透明度;真实印泥会透出下面的字 |
| `marginMm` | `20` | 命名锚点距页边的距离 |
| `maxPagesPerSeal` | `20` | 一枚骑缝章最多跨多少页 |
| `overwrite` | `false` | 是否允许写回原文件 |

**默认不覆盖原件**:盖章不可逆,而未盖章的原件正是出事时用来比对的东西。默认输出
`<原名>.sealed.pdf`。

## 它会拒绝的事

- **加密的 PDF**:pdf-lib 能打开,但写回去时那层保护就没了——"盖章成功"会变成"顺手把密码去掉了"。
  所以直接拒绝,并说明要先自行解除保护。
- **JPEG 印章**:JPEG 没有透明通道,盖上去是一个白底红圈,会把下面的条款挡住。仍然可以用,
  但结果里会明确警告。
- **单页文档盖骑缝章**:一页无缝可骑。

## 依赖

本包用 [pdf-lib](https://github.com/Hopding/pdf-lib)(MIT)。这是本仓库唯一带运行时依赖的
插件——手写一个 PDF 写入器来避免它,等于用一个没人验证过的实现去处理别人要签字的文件。

## 许可证

MIT——见 [LICENSE](LICENSE)。
