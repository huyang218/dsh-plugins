# tools/

Plugins that give the model new **capabilities** — they register tools with
`ctx.tools.register(defineTool(...))`, and the model calls them.

Writing one: the "工具(defineTool)" section of [CLAUDE.md](../../CLAUDE.md).
